<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:background="@drawable/dialog_bg"
              android:padding="0dp"
              android:orientation="vertical">

    <TextView
        android:id="@+id/message"
        android:layout_width="match_parent"
        android:layout_height="@dimen/common_dialog_message_height"
        android:gravity="center"
        android:textColor="@android:color/black"
        android:textSize="@dimen/common_dialog_btn_text_size"/>

    <View style="@style/DividerStyle"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/common_dialog_btn_height"
        android:divider="@drawable/dialog_button_divider"
        android:padding="0dp"
        android:orientation="horizontal"
        android:showDividers="middle">

        <TextView
            android:id="@+id/button_cancel"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:ellipsize="end"
            android:gravity="center"
            android:background="@null"
            android:singleLine="true"
            android:text="@string/cancel"
            android:textColor="@color/dialog_button_text"
            android:textSize="17sp"/>

        <TextView
            android:id="@+id/button_ok"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:ellipsize="end"
            android:gravity="center"
            android:background="@null"
            android:singleLine="true"
            android:text="@string/confirm"
            android:textColor="@color/dialog_button_text"
            android:textSize="17sp"/>
    </LinearLayout>

</LinearLayout>